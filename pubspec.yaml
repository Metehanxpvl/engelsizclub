name: engelsizclub
description: MetoCare — özel gereksinimli kahramanlarımız için rehber
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.16.0
  http: ^1.2.2
  url_launcher: ^6.3.1
  shared_preferences: ^2.5.5
  image_picker: ^1.2.3
  geolocator: ^14.0.3
  flutter_map: ^8.3.1
  latlong2: ^0.10.1
  flutter_tts: ^4.2.5
  google_fonts: ^8.2.0
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - src/imports/119686.png
